# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "jokers-of-neon-app"

[setup]
script = '''
set -e

ENV_SRC="$HOME/.config/jokers-of-neon/.env"

# linkea el .env compartido si no existe en el worktree
if [ -f "$ENV_SRC" ] && [ ! -e ".env" ]; then
  ln -s "$ENV_SRC" .env || cp "$ENV_SRC" .env
fi

# instala deps (mejor que npm i)
npm ci || npm install
'''

[[actions]]
name = "Run"
icon = "run"
command = "npm run dev"

[[actions]]
name = "Run https"
icon = "tool"
command = "npm run dev:https"
