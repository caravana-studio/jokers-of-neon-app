import { useEffect } from "react";
import { useGameContext } from "../providers/GameProvider";
import { getJsonFromUrl } from "../utils/loadJsonFromUrl";


export const useColor = (color: string) => {
  const { modConfig } = useGameContext();

  
};
